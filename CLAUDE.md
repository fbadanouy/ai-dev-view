# CLAUDE.md

This project's agent instructions live in **[AGENTS.md](AGENTS.md)** — read it first.

Quick reminder of the one rule that matters most here: **only real data.** Every value
shown must trace to a real field in a provider's data directory (`~/.kiro/`,
`~/.claude/`, `~/.codex/`). Never estimate, fuzzy-match, or backfill. See
[docs/DATA-CONTRACT.md](docs/DATA-CONTRACT.md).
